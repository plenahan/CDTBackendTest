<div>
    <label>Title</label>
    <input type="text" name="title" value="<%= template.title %>">
</div>
<!-- <div>
    <label>Idea</label>
    <select name="idea">
    < ideas.forEach(idea => { %>
      < if (idea.id === template.idea) { %>
        <option selected label="<= idea.title %>" value="<= idea.id %>">
      < } else { %>
        <option label="<= idea.title %>" value="<= idea.id %>">
      < } %>
    < }) %>
    </select>
  </div> -->
<!-- <div>
    <label>Publish Date</label>
    <input type="date" name="publishDate" 
        value="<= book.publishDate == null ? '' : 
        book.publishDate.toISOString().split('T') %>">
</div> -->
<!-- <div>
    <label>Page Count</label>
    <input type="number" name="pageCount" min="1" value="<= book.pageCount %>">
</div> -->
<div>
    <label>Cover</label>
    <input type="file" name="cover" class="filepond">
</div>
<div>
    <label>Description</label>
    <textarea name="description"><%= template.description %></textarea>
</div>